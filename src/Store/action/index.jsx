import { fetchAllCategory, setCategory } from './getCategory';
import { fetchAllBooking, setBooking } from './getBooking';

const allStore = {
  fetchAllCategory,
  setCategory,
  fetchAllBooking,
  setBooking,
};

export default allStore;
